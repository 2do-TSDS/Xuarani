<%= form_with(model: materium) do |form| %>
  <% if materium.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(materium.errors.count, "error") %> prohibited this materium from being saved:</h2>

      <ul>
        <% materium.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :curso_id, style: "display: block" %>
    <%= form.text_field :curso_id %>
  </div>

  <div>
    <%= form.label :orientacion_id, style: "display: block" %>
    <%= form.text_field :orientacion_id %>
  </div>

  <div>
    <%= form.label :ciclo_lectivo_id, style: "display: block" %>
    <%= form.text_field :ciclo_lectivo_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
