<%= form_with(model: asistencia_general) do |form| %>
  <% if asistencia_general.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(asistencia_general.errors.count, "error") %> prohibited this asistencia_general from being saved:</h2>

      <ul>
        <% asistencia_general.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :alumno_id, style: "display: block" %>
    <%= form.text_field :alumno_id %>
  </div>

  <div>
    <%= form.label :parametro_id, style: "display: block" %>
    <%= form.text_field :parametro_id %>
  </div>

  <div>
    <%= form.label :observaciones, style: "display: block" %>
    <%= form.text_field :observaciones %>
  </div>

  <div>
    <%= form.label :fecha, style: "display: block" %>
    <%= form.date_field :fecha %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
