<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
  <div class="container-fluid">
    <!-- BRAND -->
    <%= link_to root_path, class: "navbar-brand d-flex align-items-center fw-bold" do %>
      <%= image_tag "escudo.png", alt: "Escudo", class: "me-2", height: "32" %>
      Xuarani - Admin
    <% end %>

    <!-- BOTÓN TOGGLER (mobile) -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#adminNavbar"
      aria-controls="adminNavbar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- MENÚ -->
    <div class="collapse navbar-collapse" id="adminNavbar">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">

        <!-- Panel de Usuarios -->
        <li class="nav-item">
          <%= link_to "Panel de Usuarios", users_path, class: "nav-link" %>
        </li>

        <!-- Organización académica -->
        <li class="nav-item">
          <%= link_to "Panel de Organización", organizacion_path, class: "nav-link" %>
        </li>

        <!-- Panel de Materias -->
        <li class="nav-item">
          <%= link_to "Panel de Materias", materias_panel_path, class: "nav-link" %>
        </li>
      </ul>

      <!-- MENÚ DE USUARIO -->
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <i class="bi bi-person-circle me-1"></i><%= current_user.display_name %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
              <li><%= link_to "Mi cuenta", user_path(current_user), class: "dropdown-item" %></li>
              <li>
                <%= button_to "Cerrar sesión", destroy_user_session_path,
                              method: :delete, class: "dropdown-item text-danger" %>
              </li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Ingresar", new_user_session_path, class: "btn btn-outline-light" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
