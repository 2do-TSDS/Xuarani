<%= form_with(model: division, local: true) do |form| %>
  <% if division.errors.any? %>
    <div class="alert alert-danger">
      <strong><%= pluralize(division.errors.count, "error") %></strong> impiden guardar:
      <ul class="mb-0">
        <% division.errors.full_messages.each do |m| %>
          <li><%= m %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="card">
    <div class="card-body">
      <div class="mb-3">
        <%= form.label :nombre, 'Nombre', class: 'form-label' %>
        <%= form.text_field :nombre,
              class: ['form-control', (division.errors[:nombre].any? ? 'is-invalid' : nil)].compact.join(' '),
              required: true, autofocus: true %>
        <% if division.errors[:nombre].any? %>
          <div class="invalid-feedback d-block"><%= division.errors[:nombre].first %></div>
        <% end %>
        <div class="form-text">Ej.: A, B, C… / 1ra, 2da, 3ra...</div>
      </div>

      <div>
        <%= form.submit 'Guardar', class: 'btn btn-primary' %>
        <%= link_to 'Cancelar', divisions_path, class: 'btn btn-outline-secondary ms-2' %>
      </div>
    </div>
  </div>
<% end %>
